{
  "flow": {
    "chain": [
      {
        "name": "mainChain",
        "value": "THEN(checkCmp.tag('checkCmp1'),slotInitCmp.tag('slotInitCmp1'),priceStepInitCmp,promotionConvertCmp,memberDiscountCmp,promotionChain,couponCmp,SWITCH(postageCondCmp).to(postageCmp, overseaPostageCmp),priceResultCmp,stepPrintCmp)"
      }
    ]
  }
}
